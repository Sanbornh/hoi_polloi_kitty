<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "HoiPolloi Kitty" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <h2>HoiPolloi Kitty</h2>
      
      <%= link_to "Home", projects_url %>

      <div class="userinfo">
        <% if current_user %>
          Signed in as: <%= current_user.name %>. <%= link_to "Log out", session_path("current"), method: :delete %>
        <% else %>
          <%= link_to "login", new_session_path %> or <%= link_to "Sign up", new_user_path %>
        <% end %>
      </div>
    <hr>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>